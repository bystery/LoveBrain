<?xml version="1.0" encoding="utf-8"?>
<!-- P3-05 审计修复: 默认最小化 Accessibility。
     1. 移除 flagIncludeNotImportantViews——不再扫描全部 App 的非重要视图
     2. 只监听必要事件类型（长按 + 窗口状态变化）
     3. App allowlist 应在代码层实现，不在 XML 声明"支持全部 App" -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeViewLongClicked|typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="50"
    android:description="@string/accessibility_capture_desc" />
