# lint 预算基线：每条规则允许的最大 issue 数。
# 生成/更新：bash scripts/check_lint_budget.sh --rewrite
# 语义：任何一条超过预算 = 新增债；任何一条低于预算 = 债还掉了，必须回来把数字改小。
# 用每条规则单独记账而不是只记一个总数：修掉 3 条 UnusedResources 同时新加 3 条
# ModifierParameter 会让总数不变，只看总数就看不见这种"债务搬家"。
#
# 这份账本记的是**仓库自己欠的债**，所以每一条都得在同一份代码上量出同一个数。
# 量不出同一个数的规则不配进这里 —— 见文件末尾的 advisory 段。

AutoboxingStateCreation 6
ClickableViewAccessibility 1
DataExtractionRules 1
DefaultLocale 2
IconLauncherShape 5
IconLocation 3
InlinedApi 1
ModifierParameter 8
PluralsCandidate 3
RedundantLabel 1
ReturnFromAwaitPointerEventScope 3
StaticFieldLeak 1
SwitchIntDef 1
UnusedResources 33

# ── advisory：不进预算比对的规则（每次仍然打印条数与理由）──
# 只允许「发现来自仓库之外」的检查走这里；名单写死在 check_lint_budget.sh 的
# EXTERNAL_RULES 里，仓库自己欠的债想用这条路免检会被当场拒绝。
# 格式：advisory <规则> <理由>（理由不能空）
advisory GradleDependency 报的是"上游又有新版可升"，量的是这台机器解析到的 Maven 清单：同一句 androidx.test.ext:junit:1.1.5 声明，CI run 36019334520 说可升到 1.3.0、本机说可升到 1.2.1。条数既不随平台稳定也不随时间稳定，锁进预算等于给门禁装定时引信；要不要升依赖另行决策，不记成工程债。
advisory OldTargetApi 报的是"targetSdk 不是这台机器所知的最新"，触发与否取决于本机 SDK 里装了多新的平台（同一份代码 CI 报 1 条、本机报 0 条）。targetSdk=35 是产品决定，不是工程债；要改就按兼容性评估单独改，别借 lint 数字推动。
