# 经验（合成夹具）

<!-- 对应生产 memory/lessons.md，被 PromptBuilder.buildSuggestUserPrompt 的「需要避开的经验」段
     以 lastH1Blocks(…, 1) 取最后一个 # 块并限长 400 注入。冻结不变。 -->

## 已确认边界（避免）
- 她说"在忙"时不要连发多条追问，也不要给"你总是这样"式总结。
- 不要替她安排时间；给两个可选时段让她挑。
- 关心要落到具体一件事，不堆"多喝热水"式模板。
