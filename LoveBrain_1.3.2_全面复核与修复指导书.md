# LoveBrain 1.3.2 全面复核与修复指导书

审查日期：2026-09-21。用途：整份交给 worker 执行；包含源码复核结论、产品改法、实施顺序和验收要求。

## 0. 结论与证据边界

**1.3.2 不能作为本轮需求的合格版本验收。** 用户已报告“生成回复”和“意图”入口崩溃，核心使用链路被阻断；源码还存在事项无限追加、过宽相关性匹配、空态入口断线、反馈数据不完整等确定问题。

本次实际克隆并检查了 [bystery/LoveBrain](https://github.com/bystery/LoveBrain)，不是沿用之前审查结论。

| 项目 | 本次核实结果 |
|---|---|
| main / v1.3.2 | `4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a` |
| v1.3.1 | `bdc8088ee060f116201b9923fe4901604fd04735` |
| 版本差异 | 106 个文件变化，20,569 行新增、1,075 行删除；包括测试和资料，不能全部视作业务代码 |
| 最新 CI #32 | 单测、Lint、Release R8 构建、Compose UI 测试均成功 |
| Release #11 | 在 Verify release signing secrets 失败；后续正式 APK 构建、签名验证和发布跳过 |
| 本次设备验证 | 未连接用户设备，未取得两处崩溃堆栈，未本地运行 Android 应用或模型效果测试 |

证据：[版本比较](https://github.com/bystery/LoveBrain/compare/v1.3.1...v1.3.2)、[CI #32](https://github.com/bystery/LoveBrain/actions/runs/35491057518)、[Release #11](https://github.com/bystery/LoveBrain/actions/runs/35491082270)。

本报告覆盖版本差异、生成与保存链路、消息身份、记忆读写、持续意图、反馈、首页、悬浮窗、锦囊、共享组件和质量门禁；不是“75 个主源码文件每一行都已运行验证”的声明。UI 判断来自源码与用户实测反馈，没有冒充手机截图验收。

证据分级：**确认**＝源码或 CI 可直接证明；**高可信定位**＝调用链与现象高度吻合，需堆栈闭环；**待定位**＝用户报告成立，但尚无足够证据断言根因；**新需求**＝按本次明确要求调整产品定义。

## 1. 用户全部问题逐项回答

| 编号 | 用户问题 | 1.3.2 复核判断 | 本轮处理 |
|---|---|---|---|
| U01 | 周末见面反复进入当前状态、每轮复读 | 部分修复，未闭环。事项筛选有新增，但二字匹配、失效意图、场景旁路仍可带回旧事 | 修读写机制和历史脏数据 |
| U02 | 进行中事项重复膨胀到成千上万字 | 未解决。`mergeOngoing` 同名直接追加状态链，无实质变化检查 | 来源校验、幂等更新、活动投影与历史分离 |
| U03 | 她／我／想法辨不清 | 部分修复。有 PARTNER/USER、来源别名和 speaker 推导；仍是字符串拼接，domain 仍接 ChatMessage，subject 解析有错位 | 类型边界、可靠序列化、事实主体修正 |
| U04 | 暂时隐藏紧挨启动按钮，难看 | 源码确认 Hero 内另加了 TextButton | 合并为紧凑运行状态与次操作 |
| U05 | 快捷功能只留知识库、反馈案例 | 当前仍有五个入口 | 改成一排两个卡片 |
| U06 | 反馈案例粗糙廉价 | 确认是小 Dialog、固定列表高度、原始枚举、文本截断、嵌套导出弹窗 | 独立完整页面、详情和统一导出 |
| U07 | 空消息“点这里主动发”没反应 | 确认回归。1.3.1 有回调；1.3.2 调用传 `onEmptyAction=null` | 恢复真实入口，和主动发共用动作 |
| U08 | 生成／主动发按钮不复用、无按压感 | 确认另写一套；但源码仍有 0.96 缩放和 120ms 动画，不能宣称全部删除 | 恢复旧版视觉基线并统一组件，实测动画感知 |
| U09 | 主动发仅“在干嘛→在干嘛呀” | 当前 prompt 明确只准润色，这是产品定义限制 | 升级为主动开场，有草稿也提供不同内容方向 |
| U10 | 生成回复导致 App 崩溃 | P0，具体根因待堆栈；已找到准备阶段异常未被整体捕获的漏洞 | 先复现，修根因及失败恢复，再做后续 |
| U11 | 今日锦囊旁意图开关崩溃 | P0，高可信定位到 Service Overlay 内普通 AlertDialog | 统一面板内弹层，验证真实悬浮窗 |
| U12 | 锦囊条件苛刻、尴尬、信息污染 | prompt 强制 5–7 条、多字段、深聊／反应预测；输入还缺其要求的近期对话 | 改为少量可主动执行的日常行动建议 |
| U13 | 全面 DRY 与大文件检查 | 确认多处复制和职责集中，详见第 8 节 | 沿修复链拆职责，删除替代后的旧实现 |
| U14 | 全面 UI 风格优化 | 有统一主题令牌，但不足以保证组件和交互一致 | 使用统一组件与实机截图／录屏验收 |

历史指导书曾把主动发限定为“仅草稿润色、空草稿不调用”。**这次用户明确不满意该定义，应以本次新需求覆盖旧约束。** 保留点赞入知识库和偏好学习；点赞、复制与实际发送仍须区分。

## 2. P0：先修崩溃，拿到可使用版本

### F01：意图弹窗与悬浮窗宿主不匹配

**证据：** `FloatingService.kt:911–925` 用 `TYPE_APPLICATION_OVERLAY` 添加面板，`1146` 用 `ComposeView(this)`，宿主是 Service。`SuggestPanel.kt:637` 的意图编辑器直接使用 `AlertDialog`，没有看到独立窗口类型适配。点击意图 chip 即进入该路径。

这与无合适应用窗口 token 导致的 `WindowManager.BadTokenException` 高度吻合，但未拿到用户堆栈，不能称为已复现根因。Android 对无效窗口 token 的定义见[官方文档](https://developer.android.com/reference/android/view/WindowManager.BadTokenException)。

**修复：** 优先复用现有 `RecordSentDialog` 的面板内渲染思路，抽一个统一 `PanelModalHost`，在既有 Overlay 根容器中显示意图编辑、记忆纠正、暂停时长等内容。它不新建普通 Activity Dialog 窗口。统一遮罩、内容触摸拦截、返回／取消、焦点、键盘、滚动和可见高度。不能复制三套 Box 弹层。

同类检查：`ResultArea.kt:1132/1165` 两个普通 AlertDialog 也在悬浮窗路径，必须一并修。首页 Activity 内合法使用的 Dialog 不应机械全删。弹层放面板根部，不作为 LazyColumn 的某一 item，以免滚动使编辑器离开 composition。

**验收：** 实际启动 FloatingService → 打开意图 → 输入 → 保存 → 再开 → 关闭／取消 → 关闭面板 → 重开；键盘弹出时按钮仍可操作。记忆“不对”和“暂时别提”走相同宿主。需要真实 overlay 测试，不用 Activity 中单独渲染组件替代。

### F02：生成回复崩溃，必须独立定位

**已确认的异常边界漏洞：** `GenerationEngine.generate():224–243` 在协程中先构建 system、知识背景、来源引用、Provider 快照，之后才进入网络请求的 try/catch。准备阶段的读盘、迁移、状态写入或回调异常不在该保护范围。`OngoingContextSelector` 在“读取并构建 prompt”时还会写 cooldown 文件。该路径的异常可能逃出前台 launch；这是一处应修漏洞，但不能断言就是用户这一次崩溃的具体原因。

worker 首先在实际 Overlay 路径复现两种输入：新建空知识库、从 1.3.1 保留数据升级。记录点击→准备→加载→首卡→完成各阶段，获取 `FATAL EXCEPTION`、首个应用栈帧、版本 SHA、Android 版本、Debug/Release 类型和必要脱敏状态。若 Release 独有，使用匹配构建的 mapping 还原堆栈。

修复生成事务边界，覆盖准备、组装、Provider、网络、解析、结果发布；取消单独处理并继续传播，普通失败转成可恢复 Error，finally 只清理属于本请求的 loading/job。旧请求不得清理新请求，失败保留消息与草稿。Compose 渲染异常必须修具体布局或状态原因，不能指望协程 catch 捕获。

不要“关掉所有异常”“全局吞 Throwable”“禁用 R8”“删掉生成入口”冒充修复。不能因缺少用户日志就停工：先自行复现并修已证实漏洞；最终仍未复现时明确列出，不标完成。

**验收：** 正常生成成功；准备阶段可控 IOException、模型失败、超时、截断、停止、快速重试均不退出 App；停止后可继续生成；同一窗口打开意图与生成互不破坏。至少覆盖 Debug 与可安装 Release、空库与升级旧库。

### F03：构建可追溯性与测试缺口

CI 通过是真的，但现有 androidTest 只有 ResultArea 相关两个文件，以 `createComposeRule()` 挂载组件，没有覆盖 Service 宿主下的意图和完整生成流程；CI 还关闭了动画，不能证明用户感知的按压反馈正常。

发布失败发生在签名检查，不是 Kotlin 编译失败。核实用户安装包是否对应 `4d3aee5`；增加版本页中的 SHA／构建类型，后续报告写明测试 APK 来源与哈希。使用既有正式签名流程，不更换身份导致无法覆盖安装；不要要求用户卸载清数据以躲过升级问题。

## 3. P1：修复旧事复读与进行中事项膨胀

### F04：事项写入没有去重，旧任务还会复活

**确认位置：** `TopicRecorder.mergeOngoing():594–678`。逻辑仅按 `name == name` 找到旧项，然后无条件执行：

```kotlin
existing.chain = "$oldChain→[$timeStr]$state（当前）"
```

相同文本重复返回也追加；只有六个固定填充词会被跳过；同义改名会新增另一条；在 ended 找到同名项又会移回 active。`mergeOngoing` 没有收到真实消息来源以验证“新增／推进”是否存在。历史链既是存储又直接进入 prompt，所以存储膨胀会扩大上下文污染。

**实施：** 为已有事项建立稳定 ID，至少区分人物、事项、事件日期／场次；更新携带真实 source IDs，绑定冻结输入；状态变化保存为明确 delta，不再让模型回传整段旧链。相同 itemId＋同一证据＋同一状态重复提交必须无操作；没有新证据的措辞变化不得当进展。新事项先匹配稳定身份，语义不确定时保留独立待核对项，不靠“约／考试”等大词表强合并。完成／取消默认不可被旧输出复活，新证据明确重开才允许。

活动文件／索引保存当前状态和有限必要变更；完整历史进已有归档体系，保留可恢复原件，不简单截掉用户数据。给活动投影设可配置字符／项数预算，例如先用每项 200 字、每轮最多 3 项作为实施初值；这些是工程初值，不是用户指定硬标准。真实历史无限长也不能整条注入。

### F05：事项选择器二字匹配导致误召回，冷却规则实际冗余

**确认位置：** `OngoingContextSelector.kt` 的 `extractRawKeywords()` 提取全部两字窗口；`computeRelevanceSignals()` 用 any contains 判断。名称“周末见面”包含“周末”，消息“周末还要加班”即命中；这只能证明时间词相关，不能证明见面事项有新证据。

`isInCooldown` 仅在 `!signals.isRelevant` 时成立，而不相关项后续本来就全部跳过；相关项每次都通过。这个冷却分支并不能限制被误判相关的事项持续进入。

`realMessageEvidence` 仅凭词命中就刷新 lastEvidenceTurn，没有核对消息 ID 是否已经消费。因此重复携带同一消息也可能维持事项“新鲜”。

**实施：** 相关性与新证据分别计算。时间词、语气词、任意二字子串不得单独授权事件注入；当前明确事件／指代、本轮明确跟进指令才可授权。事件指代不确定就保守不主动带出，不能靠模型裁判每轮增加一次 API。记录已使用的真实证据身份，读取／生成／重复来源不推进证据时间。对方重提、实际新状态或用户明确要求跟进仍应正常放行，不能为了冷却而答非所问。

将 selector 改为无副作用选择，返回可解释的选择理由和可选状态变化；持久状态在既有成功事务边界处理，不能构建 prompt 就写业务进展。

### F06：失效意图从旁路重新唤醒事项

**确认：** VM 和 PromptBuilder 对持续意图有启停／到期处理，但 selector 自己重新 `readIntent(kbName)`，只判断 `enabled`，不检查 status 或 expiry，也没有使用冻结 effectiveIntent。这违反同一请求同一快照。

**修复：** 一个 `effectiveIntent` 计算入口，生成、事项选择、锦囊等所需消费者只使用其结果。已暂停、已完成、已到期均不参与检索授权；用户本轮想法以生成时最终草稿快照传入，不从旧消息重新拼另一份。不要把“长期想邀约”变成每轮必提邀约。

### F07：scene / recent / 经验仍有旁路，事项选择不是全部修复

**确认：** `buildKnowledgeInsertionWithRefs` 仍注入 `transformSceneChain(scene)` 和 recent；场景仅按时间和精确文本处理，没有与本轮相关性绑定。`transformSceneChain` 不复用写入端 StoredFact 解析，而是拆分字符串、剥去元信息。事项即使被 selector 排除，只要同一旧事还在 scene／recent，就可能重新进入上下文。

`updateSceneChain` 对无来源新事实仍添加（evidenceTime=0），外层条目却使用当前时间；读取端按外层时间显示“不到一小时前”，并剥掉来源说明。无来源内容不能因此成为看似刚发生的可信状态。

同一来源可以包含“喜欢猫”和“周末加班”两个不同事实，不能用“来源相同＋subject相同”直接认为同一事实；反之新来源可能是同一事实的推进，也不该永远只追加。

**实施：** scene 和 plan 使用共享事实身份／来源解析，当前状态是相关可信事实的投影。无来源新输出进入待核对或拒绝活跃写入，旧资料保留但不以迁移／重述时间刷新。拆开事实发生时间、证据时间与写盘时间。recent 保留真实历史身份与时间，不能改标成当前新消息；点赞学习保留，但单独标记偏好材料。当前话题标签不得由“旧标签重述”不断刷新。

**总体验收不是搜 prompt 中是否完全没有“周末”四字，而是**：旧事不以新事实或本轮主动动作授权进入；真实近期聊天可作为历史出现，但身份、时间、相关性清楚。对方真实重提见面时能接上，换聊电影时不再自己绕回约见面。

### F08：升级修复必须处理已污染的数据

修新写入不等于已累积的万字状态链会自行消失。提供一次可恢复迁移：备份旧 plan/scene 与版本标记→解析→合并完全重复状态→生成活动投影→旧链归档→原子切换。重复执行幂等；中断可恢复；未知格式原样保留并退出高可信自动注入；不能删除后假装“清理完成”。同义重复若证据不足，不自动猜合并，更不在后台给用户增加未知模型费用。

用脱敏旧格式构造至少万字重复链的升级样本，校验活动投影有界、原始档案还在、不同人的同名事项不被合并。

## 4. P1：从结构上区分她、我、想法

### F09：现有改进应保留，但类型隔离尚未落到主链

已有 `DialogueMessage(PARTNER/USER)` 和 `ReplyDirective`，这是正确方向；当前 `GenerationEngine.generate`、PromptBuilder、TopicRecorder 主接口仍然使用 `List<ChatMessage>`，各处重复过滤 IDEA。不能仅凭 `Dialogue.kt` 注释就称“类型系统已杜绝混入”。

在 UI→domain 边界生成统一不可变 `GenerationInput`：真实 dialogue、replyDirective、对象 ID、有效 intent、记忆 revision、请求 ID。UI 可继续用“她／我／想法”快速录入，进入业务链后才分离。来源查找、topic、scene、ongoing、recent 只接收 dialogue；控制信息不能作为事实证据。无真实消息但有想法应进入明确的主动开场流程，不对空真实聊天硬做“回复对方”。

### F10：字符串形式的“结构化”不足以保护角色边界

`buildChatBlockWithAliases():374–414` 把正文直接写成 `[m0] PARTNER: ${content}`。正文包含换行、`[m1] USER:`、`</chat>` 时，会造成表示层歧义；预算裁剪又用 `takeLast`，可能切掉来源与 speaker，剩下半句正文。

**实施：** 使用真实 JSON 序列化：`messages:[{id,speaker,text}]`，控制信息独立字段／区块；正文通过序列化转义。PARTNER/USER 是数据中的人物身份，不机械映射成 API 的 assistant/system 消息角色。API 对话角色表示调用协议，不能把“对方”误当模型 assistant。

预算以完整消息／记忆条目裁剪，来源映射仅保留最终真正发送的消息，最新消息不可切掉 speaker。最新单条本身超预算时给明确可恢复提示或显式摘要流程，不静默断身份。完整 final prompt 和实际 references 由同一次组装产生，不能再用文本前 80 字搜索推测注入了什么。

### F11：事实 subject 推导混用了两种叙述视角

`format.md` 要求事实用“她／我”开头，通常是用户视角摘要；`FactSubjectResolver.resolveByPronoun` 却把模型摘要中的“我”按原说话人自指解释。

例：对方说“你昨天是不是加班了”，模型按用户视角写“我昨天加班”；speaker=HER，但现规则会把 subject 判成 HER。另一个确定反例是“我妈住院了”，startsWith("我") 会把主体判成说话人本人。“你朋友…”同样不能直接算对方本人。

**实施：** speaker 仅从真实来源角色确定；subject 必须独立，结合实际证据正文与明确实体指代。保守处理亲属、第三人、引语、省略主语、多主体；不确定标 UNKNOWN，不进任何一方的确定画像。统一摘要叙述视角，不能对模型已经改写的第一人称二次转换。删掉 startsWith("我") 后一串必然冗余的 startsWith("我今天") 等分支。

角色的结构隔离能显著减少误读，但不能保证 LLM 语义永不出错。以真实例子验收，而不是添加更长“严禁混淆”提示词。

## 5. P1：首页、按钮、空态、反馈页面整体修复

### F12：首页保持原风格，只留下用户需要的快捷入口

复用既有浅蓝主色、浅灰底、近白卡片、字体层级和圆角体系，不另换主题。

Hero 内改为紧凑的运行状态与操作：未启动显示原主按钮；运行中显示状态及“打开悬浮窗”，右侧小型更多入口含“暂时隐藏”；隐藏后显示“恢复悬浮窗”。不在启动 CTA 下方再垂直叠一条抢注意力的隐藏按钮。隐藏不应删除配置或停止必要状态；恢复复用现有 WindowState 机制。

快捷功能区域只保留一排“知识库”“反馈案例”两张等宽卡片，删除空占位与多余行。“谈心／锦囊／五维”从首页快捷区移除，不等于擅自删除相关领域能力；锦囊按第 6 节在主动发中承接。

### F13：双按钮复用组件，而非复制样式

`GenerateButton.kt` 旧实现约 250 行，主源码已无调用；`LoveBrainPanelScreen.DualGenerateRow` 又独立实现 loading、stop、pressed、enabled、elapsed。这是确认的 DRY 违例。

注意：双按钮仍调用 `rememberPressScale(0.96f)`；该 helper 是 120ms 动画，还有 haptic。用户“没有按压感觉”需要实测稳定性、状态替换时机、裁剪边界和快速点击，不得把“源码里有”当验收。

抽统一 `GenerationActionButton` 与生成状态条：旧版视觉和按压作为基线，布局层只决定一排两个入口。pressed、disabled、loading、stop、retry 的尺寸／圆角／反馈共用；点击后仍即时启动请求，不为展示动画故意拖慢 API。动画节点避免被状态切换过早拆掉；用录屏比较轻点、长按后松开、快速轻点。

保留“生成回复”“主动发”两个常驻入口；只改这一条、点赞入库等已有功能继续工作。清理旧 GenerateButton 的死分支，不保留两套“以后也许用”的版本。新增代码走共享组件，不能复制后改标签。

### F14：空消息点击恢复

`MessageList` 仍显示“还没有聊天记录，点这里主动发一条”，但调用端 callback=null，proactiveActive=false。

恢复入口，调用和右侧主动发相同的模式切换／任务入口。推荐空态改为“想主动聊两句？让军师找个话题”，点击打开主动发区：有想法可输入，无草稿可找话题。不要用“改成不可点击文案”满足修复，也不能点击只切内部变量却没有可见变化。配置缺失时进入现有配置引导。

### F15：反馈案例改成可用的完整页面

当前具体问题：过滤项放同一 Row，窄屏／大字号可能拥挤；案例显示 `UNDERSTANDING_ERROR`、`PENDING` 等内部名；候选 take(60) 后没有完整详情入口；列表是 Column 全量渲染并限制 400dp；导出另开一层 Dialog；空态时导出仍像可用主按钮。

推荐实现：复用现有 Activity 页面架构与顶部栏，标题“反馈案例”＋次级数量；筛选芯片可横向滚动或换行；案例用 LazyColumn。卡片显示中文类别、候选预览、用户原因、时间，对象／模型放详情次级信息；点击详情查看完整输入、候选、反馈、期望表达、版本；只保留一个明确导出动作，格式选择在二级操作。使用系统分享／保存接口，复制提供成功反馈。加载失败与无案例分开显示。

不做炫光、复杂动画、更多仪表盘；重点是层级、留白、中文文案、可读性和操作闭环。所有触摸区域、禁用反馈和焦点符合统一组件标准；视觉小图标也要有足够触摸区域。

### F16：反馈不仅不好看，诊断资料也不完整

`FeedbackCase` 有 role/contextMode/promptVersion 字段，但 `saveFeedbackCase` 没有填；模型里没有完整真实消息快照。`SetupActivity` 直接读 cases.json，并自己实现 `buildMarkdownReport`，其导出还漏了 repository 版本包含的想法和意图。用户攒案例再交给 AI 分析时，会缺失“对方到底说了什么”的关键证据。

统一由 repository 提供查询、状态流和导出，UI 不直接读盘；点踩绑定冻结 generation snapshot 和具体候选版本，保存必要真实消息（含人物身份）、本轮想法、实际使用的意图／记忆引用、上下文模式、模型及 prompt 版本。usage 未知显示未知，不把默认 0 当已核实成本。避免记录凭证。

`FeedbackCaseRepository` 当前 mutableList 无互斥，`file.writeText` 直接覆写，错误只日志吞掉。修为序列化写入、原子落盘和可见失败；利用现有通用存储能力，不再创建一套伪成功机制。处理“刚点踩就补原因”的保存顺序，避免初始保存迟到覆盖编辑，或编辑因案例还没落盘而无操作。对同轮同候选同版本复用身份，取消再点踩不制造重复案例。

验收：连续反馈／快速编辑后重启仍在；单一导出内容一致；失败保留编辑；第三方仅看导出就能判断角色错、事实错和表达偏好问题；点踩不自动增加模型请求。

## 6. P1：重新定义主动发与今日锦囊

### F17：主动发服务于“我想聊点什么”

现状 `polish.md` 明确只用草稿、不带关系信息、不扩内容，`GenerationEngine.generateProactive` 也确实只传草稿。不能再以“更严谨地只润色”解决用户想要主动内容的需求。

新行为：

| 输入情形 | 应提供的帮助 |
|---|---|
| 有明确内容，如“今晚想约她吃饭” | 保留目标，给 2–3 个自然且有内容区别的开场；缺必要事实不编造 |
| 模糊开场，如“在干嘛” | 至少一个直接关心、一个轻问题或新话题方向；不只是呀／呢／啊替换 |
| 没有草稿 | 给少量无需对方先说特定台词的主动话题或行动建议 |
| 用户明确“只润色这句” | 仍可做保守润色，原句合适时可基本不变 |
| 对方明确要休息／不聊 | 不强行制造话题，允许建议晚点再发 |

例如“在干嘛”的候选可以是“今天过得怎么样？”、“最近有没有什么好看的，想找一部看看”。后一句涉及用户当下想找内容的表达，必须符合用户选择的方向；不能擅自编“我刚看完你推荐那部”。若选择分享猫狗／晚餐，先提示用户提供或确认真实素材，再生成配文，不能把建议写成“我刚拍了只猫”这个已发生事实。

复用生成引擎、Provider、取消、错误和候选显示能力；新增任务 mode，而非复制一整套 ViewModel/网络层。读取少量可信且相关的画像／表达偏好与真实近期对话，不把全部事项历史接回。优先一次请求完成，不加自动多轮裁判或每次二次润色。

### F18：锦囊成为轻量日常行动建议

源码 `suggest.md` 强制 5–7 条，每条 slot/topic/example/why/expected，再加 stage/goal/invite/avoid，还要求情绪深化、趣味、深度了解等配额。它容易产出“等她说某句话你再说……”和无依据反应预测。`buildSuggestUserPrompt` 实际只用画像、经验和筛选事项，却要求模型结合最近对话／场景状态链；输入合同与输出期待不一致。

**定位：** 帮用户在现实生活中找到合适的联系机会。入口承接在“主动发”的“找话题／今日建议”，默认不铺满回复主页。持续意图放独立的紧凑设置入口，文案“对话目标：未设置／已开启”，不要让用户以为这是锦囊开关。

默认最多展示 3 条简洁行动建议，每条只显示“什么机会＋怎么做”；示例配文和理由点击展开。取消强制 expected、升温目标、深聊配额和默认长篇避雷。换一批保留，已选建议可进入主动发准备区，但不能直接记为真实聊天。

建议的日常内容应类似：

- 早上自然打个招呼：关系适合且没有未回消息时，可以一句早安；不是每日强制签到。
- 路上真遇到有意思的猫狗：顺手拍一下分享，图片本身就是内容，不需要硬接情话。
- 晚饭有值得分享的东西：发照片＋一句真实感受，不要求对方回答长问题。

这些是设计范例，不是所有关系固定套模板。既可给不需准备的轻话题，也可给需要真实素材的行动；明确区分“现在能发”和“遇到时再做”。没有素材时可建议行动，不能假装素材已经存在。对方明确要空间时，不推一堆每日关心任务。

建议可沿用 `tips` 容器迁移为 action/timing/materialNeeded/example/reason 等语义字段；输出模型、解析器、流式解析和 UI 一次同步修改，兼容旧缓存或明确失效，不做只有新 prompt、旧 parser 的半套变更。

验收侧重：用户不用等对方说指定句子就能使用；大部分建议能主动决定去做；不需要装人设、不需要背台词；内容少而清晰。不给“她一定会开心”一类伪确定预测。

## 7. 其他必须一起闭环的问题

### F19：记忆纠正粒度过大

`makeRef` 使用 `kind:sourcePath` 文件级 ID。ongoing 的多个事项共用 `ONGOING:moment/plan.md`；某条“结束／别提”实际会过滤整段，不是那一件事；文本展示又只 take(500)。

修为条目级稳定 ID，来源和引用范围明确；修正 A 不影响 B。档案整体操作可以保留，但 UI 必须说清“整份档案”，不能伪装为“这条”。纠正后的数据不得从 scene、recent 摘要或 lessons 旁路恢复为可信事实；使用实际来源追踪，未知旧衍生项保守标识而非装作已证明依赖。

### F20：Prompt 预算与知识构建有重复实现

`applyBudgetByBlocks` 名为按块，实际有 knowledge.take、chat.takeLast，且补省略标记后可能继续超预算。`buildReplyUserPromptOnlyThisRound` 直接拼接，未使用同一预算路径。修成共享 section/message 结构裁剪和预算统计；想法、最新消息完整保留，过大时显式处理，不切角色与 JSON。

`buildKnowledgeInsertion` 和 `buildKnowledgeInsertionWithRefs` 两套主体高度重合，后者又手写 profile/style，另有 appendProfileSection。一个构建入口同时产生文本和 refs，不要维护“有引用版”和“无引用版”两套规则。场景读写也共用 codec，减少不同解析口径。

### F21：保存幂等只保护了部分步骤

`TopicRecorder.record` 检查 alreadyRecorded 只跳过 recent 和 turn count；topic rotate 和 mergeOngoing 仍执行。失败重试时可能再次轮换、再追加状态链。已有 `rotateTopic` 自身的恢复机制应保留，但不能代替整轮保存的幂等。

为完整 round commit 建立稳定事务身份，recent/scene/plan/topic/turn 一致提交或可恢复重放；各 delta 自身也幂等。不要把逐文件 atomicWrite 误当成整个业务事务原子。覆盖中间步骤失败、重启后恢复、同轮二次保存。

### 已有正确改进，必须保留

已看到 ProfileUpdate 对 finish_reason=length／不完整 JSON 的分类、重试逻辑、ProfileUpdateSchema 单一描述来源，以及 KnowledgeRepository 锁内 revision 检查和原子写入／回滚能力。不得复制旧报告断言这些“完全没做”。本轮未实际运行全链路画像验收，不能反向夸成“绝不截断”。

点赞正文偏好保存、候选不自动当发送、only-this-round、单条改写、语音改写、对象隔离、用户手动纠正等能力应保留，并随受影响改动做有针对性的回归。不要为了瘦身删除用户认可的功能。

## 8. DRY 与大文件整改范围

| 文件 | 当前行数 | 应拆的职责／复用点 |
|---|---:|---|
| LoveBrainViewModel.kt | 2,591 | 请求生命周期、轮次快照／历史、反馈、单条改写、意图、实际发送分别有明确协调职责；VM 保留 UI 状态组合 |
| KnowledgeRepository.kt | 1,893 | 文件原子写、知识库生命周期、画像事务、意图／纠正存储、迁移／归档；共享锁与事务所有权不能拆丢 |
| SetupActivity.kt | 1,587 | 首页、Provider 编辑、无障碍说明、反馈页面、导出；UI 不直接维护反馈 JSON |
| ResultArea.kt | 1,323 | 结果布局、工具入口、记忆引用／纠正、加载；弹层用统一宿主 |
| LoveBrainPanelScreen.kt | 1,238 | 屏幕编排；移出重复按钮、独立状态条和弹层内容 |
| DeepSeekRepository.kt | 1,121 | Provider 快照、请求／流解析、错误分类；本轮改动需遵循现有边界，不为行数强拆 |
| PromptBuilder.kt | 993 | 结构化输入、共享知识段、预算、refs；删平行构建实现 |
| SuggestPanel.kt | 843 | 建议列表、行动卡、意图编辑；重设信息层级 |
| TopicRecorder.kt | 738 | round commit、scene codec、事项 delta reducer、历史投影 |
| SchemeCard.kt | 736 | 卡片呈现、改写菜单、语音交互边界，复用现有 VoiceRewrite |

行数本身不是 bug；这里的风险在于状态、存储、网络与 UI 混杂，改一处难以知道影响范围。禁止为了“每文件不超过 X 行”机械切碎。

已证实的重复优先清理：生成按钮两套；反馈读写／导出两套；知识插入两套；scene 两种解析；意图有效性多处；角色过滤和转换多处；多处手写相近弹层。

执行方式：随 F01–F21 抽出必要职责，每一步保留行为测试；同一业务规则一个实现，其他调用者依赖它。不要先做几天“大重构”再修崩溃，也不要仅移动代码、保留平行旧逻辑就宣称 DRY 完成。独立的少量 UI 组合不必为了复用而做万能组件。

## 9. 全 UI 检查矩阵

| 页面／状态 | 修改重点 | 必须看的实际状态 |
|---|---|---|
| 首页 Hero | 状态和主次操作统一，隐藏不再独占一行抢主按钮 | 停止、运行、隐藏、恢复、权限缺失 |
| 快捷功能 | 知识库＋反馈案例两卡 | 小屏、大字、滚动 |
| 消息录入 | 角色可辨，想法有独立语义，编辑保持角色 | HER/ME/IDEA、编辑／未提交、多行 |
| 双生成按钮 | 旧基线、统一组件、明显但克制的反馈 | 轻点／长按、禁用、准备、生成、停止、错误 |
| 结果卡 | 不因增功能把主内容挤走 | 首卡、长文本、风格／方向、单条修改／语音 |
| 弹层 | 面板内统一宿主与焦点 | 意图、纠正、暂停、实际发送，键盘和返回 |
| 反馈案例 | 完整页面、中文、详情、统一导出 | 空、加载、失败、长内容、分类、导出 |
| 主动发 | 草稿可选、内容方向有区别 | 空草稿、有草稿、想法、缺素材、停止 |
| 今日建议 | 默认少量、理由折叠、无信息墙 | 3 条以内、展开、换一批、进入草稿 |
| 知识库／编辑／配置／谈心 | 复用同套页头、按钮、提示、滚动规则；不额外改业务定义 | 长档案、输入错误、加载失败、小窗口 |

必须提交改前／改后同尺寸实机截图；按压和窗口切换用短录屏。至少普通字号和放大字号、默认与较小悬浮窗、键盘打开与关闭。设计图、颜色常量相同、组件测试通过，都不能代替可读性与交互验收。

## 10. 执行批次：按此顺序连续推进

1. **恢复可用性：** F01–F03 两处崩溃／异常恢复／真实 Overlay smoke，同时修 F14 空态死入口。产出可安装、可追溯版本，不能只提交代码。
2. **修污染根因：** F04–F11、F19–F21。先共享输入／事实／事务边界，再去重、过滤、迁移；用旧数据证明修复。
3. **产品与 UI：** F12–F18。恢复统一按钮、两卡首页、完整反馈页面、实质主动开场、轻量日常建议；同步改数据合同与解析。
4. **收尾：** 清理被替代实现、死代码、过时注释和过时测试预期；完成矩阵，提交完整差异与安装包说明。

每批做必要检查并继续，不因为常规实现选择反复询问用户。确有外部缺失（例如只有用户设备才出现的堆栈）列清楚，但继续完成可独立推进的工作。不要自行改变发布／签名策略，不要为完成指标删除失败测试。

## 11. 最小但有针对性的验收用例

| 用例 | 输入／动作 | 通过条件 |
|---|---|---|
| T01 | 真 Overlay 点击生成→加载→首卡→完成 | App 不崩、能复制、能停止重试 |
| T02 | 真 Overlay 意图编辑、纠正／暂停弹层 | 无窗口异常，键盘与取消可用 |
| T03 | 准备阶段 I/O 故障，随后恢复 | 留在 App、保留草稿、明确错误、后续可生成 |
| T04 | 旧版本数据升级后重复 T01/T02 | 不要求清库或重装 |
| T05 | 旧项周末见面，本轮“周末还要加班” | 不把见面当新证据／强行追问 |
| T06 | 已谈见面→连续聊电影 5 轮 | 不反复带回见面，状态证据时间不自刷新 |
| T07 | 她重提“上次说的见面几点” | 正常召回相关事项，允许更新 |
| T08 | 相同 source＋state 连续保存 50 次 | 活动内容和变更条数不增长 |
| T09 | 已完成事项，模型重报进行中 | 无新真实证据不复活 |
| T10 | 过期／暂停／完成意图仍 enabled | 意图不注入，事项不由它唤醒 |
| T11 | 万字旧 plan＋未知格式迁移，中断重试 | 活动投影有界、原件保留、迁移幂等 |
| T12 | 一句含两个事实，同 subject/source | 两个事实都保留，不互相覆盖 |
| T13 | HER“你昨天加班？”→摘要“我昨天加班” | 不错判为 HER 的事实；疑问不当肯定证据 |
| T14 | “我妈住院”“你朋友考试”“她说我…” | 不把第三人／引语直接变我或她的事实 |
| T15 | 正文含换行、`[m1] USER:`、`</chat>` | 仍是一条原人物消息，来源映射不伪造 |
| T16 | 超预算、多行消息、仅看本轮 | 完整条目裁剪、最新身份和想法保留 |
| T17 | 结束／静音事项 A，同库事项 B 仍有关 | 只影响 A，重启／换题后仍正确 |
| T18 | 保存中间步骤失败后再试 | 无重复状态、无二次错误轮换、无丢数据 |
| T19 | 空消息点击提示 | 进入主动发，界面有明确响应 |
| T20 | 双按钮轻点与长按松开录屏 | 按压可见、布局不抖、一次点击一次请求 |
| T21 | “在干嘛”、空草稿、有明确目标 | 至少两个内容方向有意义区别，不靠语气词凑数 |
| T22 | 无猫狗／晚饭素材却选分享建议 | 引导准备素材，不生成虚假既成经历 |
| T23 | 日常建议 | 默认≤3条、无需对方指定台词、理由折叠 |
| T24 | 连续点踩＋立即编辑＋重启＋导出 | 数据完整、一致、中文可读、能追溯输入 |
| T25 | 多库切换、迟到请求、意图修改 | 不串对象，旧请求不覆盖新设置或结果 |
| T26 | 点赞多候选、只复制、确认实际发送 | 偏好可学习；点赞／复制不变成已发送事实 |
| T27 | 画像截断与重试 | 旧画像不被半截 JSON 覆盖，错误可恢复 |
| T28 | 普通／大字号＋小窗口＋键盘 | 输入、主按钮、结果和弹层都可访问 |

T05/T06/T07/T13/T14/T21/T23 需要代表性真实模型输出抽查；单测只能验证边界和算法，不能证明自然度。保存实际输入、关键请求段、输出和 token 使用，不凭“prompt 加过了”验收。建议先用 12–20 组固定代表场景，修具体失败，不扩成无止境评测工程。

保留仓库门禁：`:app:testDebugUnitTest`、`:app:lintDebug`、`:app:assembleRelease`、`:app:connectedDebugAndroidTest`；补测试应直接覆盖上述风险，勿加仅搜索源码字符串的“机制存在”测试。Overlay 冒烟需显式启动 Service 和授权流程，不能只是组件测试。

## 12. 给 worker 的交付要求

你收到本文件即开始工作。先记录当前 HEAD；若已不同于审查 SHA，先核对增量，不回滚后来正确修复。以本次用户需求为最高依据，旧文档“主动发只润色”等相反约束作废。

交付必须包含：

1. 每项 U01–U14／F01–F21 的处理状态、对应提交和实现位置；未完成如实标记。
2. 两个崩溃各自的复现步骤、堆栈、根因、修复后证据；不能拿一个修复覆盖两个未证明相同的崩溃。
3. 旧状态数据的备份、迁移、恢复说明；确认未删除用户真实历史。
4. 新旧 UI 同尺寸截图和双按钮按压录屏，包含真实 Service 悬浮窗。
5. 真实模型代表样例与实际 token；重点展示周末见面不复读、角色不串、主动发不止加语气词、建议可主动执行。
6. 测试命令、结果、SHA、APK 来源及哈希；CI 构建通过与签名发布成功分别说明。
7. 被删除／合并的重复实现清单，证明已形成单一业务规则入口。

完成标准是用户可以顺畅录入、生成、主动找话题，并且记忆不会越用越污染；不是新增文件数、注释里的“已闭环”或测试数量。

## 附：核心源码证据链接

以下链接全部固定到本次审查 SHA；后续修改不改变本报告引用的版本。

- [生成准备与请求异常边界](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/domain/GenerationEngine.kt#L203)
- [Service 悬浮窗宿主](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/service/FloatingService.kt#L911)
- [意图普通 AlertDialog](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/ui/panel/SuggestPanel.kt#L621)
- [事项状态链追加](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/domain/TopicRecorder.kt#L594)
- [事项相关性与冷却](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/domain/OngoingContextSelector.kt#L99)
- [角色序列化与知识注入](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/domain/PromptBuilder.kt#L374)
- [事实主体推导](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/domain/FactSubjectResolver.kt#L57)
- [空态断线与双按钮](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/ui/panel/LoveBrainPanelScreen.kt#L357)
- [反馈页面与首页入口](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/ui/SetupActivity.kt#L449)
- [反馈存储](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/java/com/lovebrain/app/data/FeedbackCaseRepository.kt)
- [主动发当前合同](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/assets/engine/polish.md)
- [锦囊当前合同](https://github.com/bystery/LoveBrain/blob/4d3aee57cd3a44c8870c9bc4c04c3c022fe8bf4a/app/src/main/assets/engine/suggest.md)
